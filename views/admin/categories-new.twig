{% extends 'layouts/layout.twig' %}

{% block content %}
<div class="container-fluid">
    <div class="row flex-nowrap">
        {% include 'layouts/admin-menu.twig' %}
        <main class="col py-3">
            <div class="container-fluid container-md p-5">
                <h1>Nouvelle catégorie</h1>
                <form enctype="multipart/form-data" {% autoescape %}action="{{ constant('SITE_ADRESS') }}/adminCategory/newProcess//{{ CSRF }}"{% endautoescape %} method="POST" class="row g-3">
                    <div class="col-12">
                        <label for="title" class="form-label">Nom</label>
                        <input type="text" class="form-control shadow" id="title" name="category">
                    </div>
                    <div class="col-12">
                        <button type="submit" class="btn btn-primary">Valider</button>
                    </div>
                </form>
            </div>
        </main>
    </div>
</div>  
{% endblock %}