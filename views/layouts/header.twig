<header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-marine"> 
            <div class="container-fluid">
                {% autoescape %}
                <a class="navbar-brand" href="{{ constant('SITE_ADRESS') }}">
                    <img src="{{ constant('SITE_ADRESS') }}/public/images/logo80px.png" alt="Logo">
                    {{ constant('SITE_NAME') }}
                </a>
                {% endautoescape %}
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            {% autoescape %}
                            <a class="nav-link" href="{{ constant('SITE_ADRESS') }}">Accueil</a>
                            {% endautoescape %}                           
                        </li>
                        <li class="nav-item">
                            {% autoescape %}
                            <a class="nav-link" href="{{ constant('SITE_ADRESS') }}/articles/page-1">Articles</a>
                            {% endautoescape %}
                        </li>
                        <li class="nav-item">
                            {% autoescape %}
                            <a class="nav-link" href="{{ constant('SITE_ADRESS') }}/portfolio">Portfolio</a>
                            {% endautoescape %}                        
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Connexion/Inscription</a>
                        </li>
                    </ul>
                </div>  
            </div>
        </nav>
</header>