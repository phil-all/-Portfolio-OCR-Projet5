{% if admin == true %}
    {% autoescape %}
        <a class="nav-link" href="{{ constant('SITE_ADRESS') }}/admin_dashboard">
            <button type="button" class="btn btn-danger">
                <i class="bi bi-lightning-fill"></i> ADMIN
            </button>
        </a>
    {% endautoescape %}
{% endif %}